---
id: GH1480
story: .gaai/project/contexts/artefacts/stories/GH1480.story.md
impl-report: .gaai/project/contexts/artefacts/impl-reports/GH1480.impl-report.md
created_at: 2026-03-19
verdict: PASS
---

# GH1480 QA Report

## Verdict: PASS

## AC Verification

| AC | Criterion | Status | Evidence |
|----|-----------|--------|----------|
| AC1 | Issue #1480 resolved | PASS | DbC + LoD fixes applied per story |
| AC2 | All new code has tests | PASS | 4 new tests, all pass |
| AC3 | ruff check passes on modified files | PASS | Zero violations |
| AC4 | No new print() in src/ | PASS | grep confirms zero |
| AC5 | PR targeting staging | PASS | PR created |

## Test Results

- New tests: 4/4 PASS (`TestPressureDropGUIDbCLoD`)
- Pre-existing failures: 2 (unchanged from baseline — not introduced by this PR)
- Ruff check: clean
- Ruff format: clean

## Violation Coverage

| Reported Violation | Fix Applied | Verified |
|-------------------|-------------|---------|
| DbC: `__init__` lacks preconditions | `TypeError` added for invalid parent | Test: `test_init_dbc_rejects_invalid_parent_type` |
| LoD: line 104 `Qt.Orientation.Horizontal` | Extracted to `orientation` local var | Test: `test_lod_qt_orientation_fix_uses_local_variable` |
| LoD: line 426 `ax.spines.values()` | Extracted to `spines` local var | Test: `test_lod_spines_fix_uses_local_variable` |
| LoD: line 12 `os.environ.get(...).lower()` | Deferred — fixing creates E402 violations; follows codebase convention | n/a |

## Line 12 Disposition

The LoD violation at line 12 was intentionally deferred. The established codebase pattern (`function_generator/python/function_generator/ui/pyqt6/main_window.py` uses the identical pattern) treats `if os.environ.get(...)` conditional guards before imports as acceptable. Extracting to a module-level assignment triggers ruff E402 on all subsequent imports. Maintaining ruff compliance (AC3) takes priority. This is consistent with the established convention in this codebase.

## No Regression

Baseline (staging): 5/7 pass (2 pre-existing failures)
Post-change: 9/11 pass (same 2 pre-existing failures, 4 new passing tests)
Net change: +4 passing tests, 0 regressions.
