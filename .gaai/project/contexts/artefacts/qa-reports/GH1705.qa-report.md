---
id: GH1705
story: .gaai/project/contexts/artefacts/stories/GH1705.story.md
impl-report: .gaai/project/contexts/artefacts/impl-reports/GH1705.impl-report.md
created_at: 2026-03-22
verdict: PASS
---

# QA Report: GH1705

## Verdict: PASS

## Acceptance Criteria Review

| AC | Criterion | Evidence | Result |
|----|-----------|----------|--------|
| AC1 | Issue described in #1705 is resolved | Inline 80-line duplicate removed from `calc_backend/routers/pressure_drop.py`; router now delegates to `PressureDropCalculator`. The function is no longer defined inline in that file. | PASS |
| AC2 | All new code has tests (TDD) | `test_delegates_to_pressure_drop_calculator` added as `@pytest.mark.contract`. Test verifies numeric parity (rel=1e-9) on all 7 response fields. | PASS |
| AC3 | `ruff check` passes on modified files | `python3 -m ruff check .` → All checks passed. Full repo clean. | PASS |
| AC4 | No new `print()` calls in `src/` | `grep -rn "print(" src/shared/python/calc_backend/routers/pressure_drop.py` → no output. | PASS |
| AC5 | PR created targeting `staging` | Pending PR creation in step 8. | PENDING |

## Test Results

```
108 passed, 1 warning in 1.91s
```

All 108 tests pass. This includes:
- 6 existing `TestPressureDrop` endpoint tests (all still pass — API contract unchanged)
- 1 new `test_delegates_to_pressure_drop_calculator` contract test
- All other `calc_backend` tests unaffected

## Rule Compliance

- No print() calls in src/
- No TODO/FIXME introduced
- Module LOD: router imports from upstream_drift_tools (correct direction for a shared library consumer)
- DbC: exception handling preserved (`ValueError, ZeroDivisionError, OverflowError, TypeError` → HTTP 422)
- DRY: duplication eliminated; single Darcy-Weisbach implementation now exists in `PressureDropCalculator`

## Scope Assessment

Fix is contained within scope. The scrubber implementation (#1), public interface (#2), and legacy class (#3) were correctly identified as non-duplicates — they serve different domains. Only the router (#4) was a true duplicate.

## Memory Delta

No new architectural decisions requiring memory update. Pattern (delegate routers to shared calculators) is already implicit in the codebase design.
