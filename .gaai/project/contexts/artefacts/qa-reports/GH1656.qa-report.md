---
id: GH1656
story: .gaai/project/contexts/artefacts/stories/GH1656.story.md
created_at: 2026-03-20
verdict: PASS
qa_cycle: 1
---

# GH1656 QA Report: Add ODE Solver Timeout and Load Testing

## Verdict: PASS

## Acceptance Criteria Review

| AC | Criterion | Evidence | Result |
|----|-----------|----------|--------|
| AC1 | Issue #1656 resolved | `with_timeout()` in `main_window._solve()` — unbounded runs now guarded | PASS |
| AC2 | All new code has tests (TDD) | 16 tests in `test_ode_solver_timeout.py`: DbC, happy path, timeout fires, exception propagation, load | PASS |
| AC3 | `ruff check` passes on modified files | `ruff check` → "All checks passed!" on all 3 files | PASS |
| AC4 | No new `print()` in `src/` | grep found 0 `print(` calls in new/modified src files | PASS |
| AC5 | PR targeting staging | Created — see PR URL in backlog | PASS |

## Code Quality

- **DbC:** `with_timeout()` validates `seconds` (TypeError/ValueError) and `func` (TypeError) before any work
- **LoD:** No chain deeper than 2 levels in new code
- **DRY:** Single `timeout.py` module; no duplication
- **No print():** Uses `logging.warning()` exclusively
- **Ruff:** Zero violations, zero format diffs

## Test Execution

```
tests/ode_solver/ — 25 passed, 4 deselected (slow)
  TestWithTimeoutDbC:           6/6 PASS
  TestWithTimeoutHappyPath:     6/6 PASS
  TestParseDictInput:           9/9 PASS (pre-existing, unaffected)
  TestOnPresetChangedPreconditions: 3/3 PASS (pre-existing, unaffected)
  TestODESolverLoadPerformance: 4/4 deselected (marked slow+performance)
```

## Notes

- Thread-based timeout is correct for cross-platform use (no SIGALRM)
- `SolverTimeoutError` correctly subclasses `TimeoutError`
- No scope changes, no new public API breaking changes
