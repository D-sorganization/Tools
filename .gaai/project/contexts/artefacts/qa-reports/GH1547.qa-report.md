---
id: GH1547
title: "Enforce required shared Python provider-contract suite in CI"
created_at: 2026-03-19
agent: qa.sub-agent
verdict: PASS
---

# QA Report: GH1547

## Verdict: PASS

All acceptance criteria met. No rule violations detected. No remediation required.

## Per-Criterion Results

| AC | Criterion | Result | Evidence |
|----|-----------|--------|----------|
| 1 | Breaking import or minimal-runtime regression in exported packages fails CI | PASS | New step runs pytest on all four exported-package test directories; collection failure or test failure exits non-zero → CI fails |
| 2 | Shared-package changes always run provider-contract tests | PASS | Step has no `if:` guard; always-on within `tests` job regardless of changed files |
| 3 | Contract coverage is visible in CI output | PASS | `--cov-report=term` produces per-file table; `$GITHUB_STEP_SUMMARY` entry appended |
| 4 | Issue #1547 resolved | PASS | Dedicated step closes the CI gap described in the issue |
| 5 | All new code has tests (TDD) | PASS | No new source code; CI config only |
| 6 | `ruff check` passes on modified files | PASS | No Python files modified; ruff reports "All checks passed" |
| 7 | No new `print()` calls in `src/` | PASS | No `src/` changes |
| 8 | PR created targeting `staging` | PENDING | Orchestrator action post-QA |

## Rule Compliance

- No `print()` in `src/` — confirmed (no `src/` changes)
- Ruff: no violations
- No TODO/FIXME introduced
- No manifest changes required (no new modules)
- Contract tests not broken

## Local Test Run

```
597 passed, 4 warnings in 58.74s
Total coverage: 25.50% (gate: ≥10%) — PASSED
```

## Remediation Attempts

None — clean PASS on first attempt.
