---
id: GH1487
story: .gaai/project/contexts/artefacts/stories/GH1487.story.md
created_at: 2026-03-19
verdict: PASS
---

# GH1487 QA Report

## Verdict: PASS

## Acceptance Criteria Review

| AC | Criterion | Status |
|----|-----------|--------|
| AC1 | Issue #1487 resolved — all DbC and LoD violations addressed | PASS |
| AC2 | All new code has tests (TDD) — 10 new tests added | PASS |
| AC3 | `ruff check` passes on modified files | PASS |
| AC4 | No new `print()` calls in `src/` | PASS |
| AC5 | PR created targeting `staging` branch | PENDING (PR creation step) |

## Violation Remediation

### DbC Violations Fixed (5/5)

| File | Function | Before | After |
|------|----------|--------|-------|
| `signal_bus.py` | `__init__` | No preconditions | `ValueError` for None func_gen/toolkit/poly_gen |
| `signal_bus.py` | `_on_func_gen_signal` | `assert signal is not None` | `ValueError` if signal is None |
| `signal_bus.py` | `_on_poly_generated` | `assert joint_name is not None` | `ValueError` (None joint_name) + `TypeError` (non-list coeffs) |
| `signal_bus.py` | `_status` | `assert message is not None` | `ValueError` if message is None |
| `main_window.py` | `_on_poly_fallback` | `assert joint_name is not None` | `ValueError` (None joint_name) + `TypeError` (non-list coeffs) |

### LoD Violations Fixed (3/3)

| File | Lines | Before | After |
|------|-------|--------|-------|
| `main_window.py` | 111, 117, 125 | `action.triggered.connect(slot)` | `_connect_action(action, slot)` |

## Test Results

- New tests added: 10 (TestSignalBusDbC × 5, TestMainWindowDbCLoD × 5)
- New tests passing: 10/10
- Pre-existing tests: 21/22 pass; 1 fails (`test_launcher_dependency_check`) — pre-existing defect, `launch_pyqt6.py` missing `check_dependencies()` function. Not introduced by this story.
- `ruff check`: PASS (zero violations)
- `ruff format --check`: PASS (zero diffs)
- No print() calls added to src/

## Additional Improvements

- Fixed pre-existing test infrastructure issue: outer `__init__.py` now bootstraps the inner `python/` path on import (mirrors `urdf_builder_gui` pattern)
- Added `conftest.py` files for proper pytest path setup
- Fixed test module's sys.path resolution to reliably find the inner package over the outer namespace package

## Memory Delta Notes

- Pattern established: outer namespace package `__init__.py` bootstrap pattern for `python/` subdirectory submodule resolution
- Pattern: use `_connect_action()` module-level helper to avoid LoD violations on Qt signals
