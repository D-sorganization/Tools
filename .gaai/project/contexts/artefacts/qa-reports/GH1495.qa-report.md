---
id: GH1495
artefact_type: qa-report
story: .gaai/project/contexts/artefacts/stories/GH1495.story.md
created_at: 2026-03-19
verdict: PASS
---

# GH1495 QA Report

## Verdict: PASS

All acceptance criteria met. No rule violations detected.

## Acceptance Criteria Results

| # | Criterion | Result | Evidence |
|---|-----------|--------|---------|
| AC1 | The issue described in #1495 is resolved | PASS | All LoD violations addressed: `component.shape` extracted in metrics, `.parent` extracted in step_export, `sys.stdout` extracted in CLI. DbC violations already resolved by PR #1500 (assertions verified in code). |
| AC2 | All new code has tests (TDD) | PASS | 23 unit tests in `tests/vessel_drafter/test_dbc_lod.py` covering all modified functions. 23/23 pass. |
| AC3 | `ruff check` passes on modified files | PASS | `python3 -m ruff check <modified files>` → `All checks passed!`. `ruff format --check` → `6 files already formatted`. |
| AC4 | No new `print()` calls in `src/` | PASS | No print() in modified src/ files (grep confirmed empty). |
| AC5 | PR created targeting `staging` branch | PENDING | Will be created by Orchestrator (QA confirms readiness). |

## Test Results

```
tests/vessel_drafter/test_dbc_lod.py: 23 passed in 0.16s
```

Full unit test suite (excluding pre-broken packages):
```
3775 passed, 23 skipped, 15 warnings in 33.94s
```
No regressions vs staging baseline.

## Rule Compliance

- No `print()` in `src/` — PASS
- No TODO/FIXME without tracked issue — PASS (none added)
- Ruff check zero violations — PASS
- Ruff format check — PASS
- Tests in `tests/` tree — PASS
- No scope creep — only files listed in story modified

## Coverage Note

Tests cover:
- `_component_metric`: numerical correctness, scalar passthrough, shape protocol (7 tests)
- `_mm3_to_in3` / `_mm2_to_ft2`: unit conversion anchors (4 tests)
- `export_default_layout_step`, `export_cylindrical_bath_layout_step`, `export_vessel_drafter_step`: DbC (3 tests), LoD/mkdir behaviour (5 tests)
- `export_vessel`: DbC assertion (1 test)
- CLI `main()`: stdout LoD fix with 3 command paths (3 tests)

## Memory Alignment Check

No memory index exists (`contexts/memory/index.md` absent). No prior decisions applicable.
No memory delta warranted.
