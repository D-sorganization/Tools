---
id: GH1697
story: .gaai/project/contexts/artefacts/stories/GH1697.story.md
plan: .gaai/project/contexts/artefacts/plans/GH1697.execution-plan.md
created_at: 2026-03-22
status: complete
---

# Implementation Report: GH1697

## Summary

- Added T201 (print detection) to `ruff.toml` `select` list so print() calls in `src/` are caught by delta CI checks going forward.
- Added per-file-ignores for `launch.py`, `verify_launcher.py`, and `tests/**/*.py` which legitimately use print() for CLI output and test assertions.
- Created `.github/workflows/nightly-full-repo-quality.yml` — a non-blocking weekly scheduled workflow that runs full-repo ruff lint, ruff format check, mypy type check, print() audit, and type:ignore comment count. All steps use `continue-on-error: true`. Violations reported as GitHub Step Summary metrics only.

## Files Changed

| File | Operation | Purpose |
|------|-----------|---------|
| `ruff.toml` | MODIFY | Add T201 to select list; add per-file-ignores for launcher/test files |
| `.github/workflows/nightly-full-repo-quality.yml` | CREATE | Non-blocking nightly full-repo quality audit (GH1697) |

## Acceptance Criteria Coverage

| AC | Description | Status |
|----|-------------|--------|
| AC1 | Issue resolved (full-repo CI job added) | PASS — nightly-full-repo-quality.yml created |
| AC2 | All new code has tests (TDD) | PASS — only YAML and TOML files changed; no Python code added |
| AC3 | `ruff check` passes on modified files | PASS — `ruff check .` → All checks passed |
| AC4 | No new `print()` calls in `src/` | PASS — zero new print() calls from this PR; pre-existing ones tracked by nightly job |
| AC5 | PR created targeting `staging` | PENDING — PR to be created |

## Key Decisions

1. **T201 per-file-ignores:** Added ignores for `launch.py`, `verify_launcher.py`, and `tests/**/*.py` to prevent false-positive failures on these files in the delta check.
2. **Non-blocking initially:** All nightly job steps use `continue-on-error: true` per the story's recommendation — allows measuring baseline violation counts before enforcing.
3. **Weekly cadence:** `cron: "0 3 * * 0"` — Sunday 3 AM UTC, matching the Code-Metrics.yml weekly pattern.
4. **Five audit types in one job:** ruff lint, format check, mypy, print() audit, type:ignore count — addresses all four issues identified in the story finding.

## Self-Validation

```
$ python3 -m ruff check .
All checks passed!

$ git diff HEAD~1 --name-only
.github/workflows/nightly-full-repo-quality.yml
ruff.toml
```

No ruff violations. No new print() calls in src/. Two files changed, both non-Python.
