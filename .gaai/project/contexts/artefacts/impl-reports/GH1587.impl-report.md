---
id: GH1587
artefact_type: impl-report
story: .gaai/project/contexts/artefacts/stories/GH1587.story.md
created_at: 2026-03-19
---

# GH1587 Implementation Report

## Summary

- Created 44-test suite for `humanoid_character_builder.mesh.primitive_inertia`, a genuinely
  untested shared-library module with pure-math inertia calculations for robotics URDF generation.
- Conducted comprehensive gap analysis revealing the original issue (#1587) overstated the
  coverage gap due to incomplete analysis methodology (missed embedded test dirs + `tests/`).
- Identified the real uncovered modules in the shared library and targeted the highest-impact
  testable one.

## Gap Analysis Findings

| Claim in Issue #1587 | Actual State |
|---------------------|-------------|
| 630/743 modules with no test | ~200 have no test (embedded src/ tests missed) |
| signal_toolkit — key gap | Has 163 tests in tests/shared/python/signal_toolkit/ |
| web_applications — key gap | Has embedded tests in src/web_applications/*/tests/ |
| model_generation — key gap | Has tests in tests/shared/python/model_generation/ |
| Calculator backends — key gap | Process calculators have tests in tests/shared/python/process_calculators/ |

The issue used only `test_<module>.py` name matching against embedded src/ directories,
missing the separate `tests/` tree with 195 test files.

## Real Gaps Identified (Shared Library)

Modules genuinely without any test coverage:
- `humanoid_character_builder/mesh/primitive_inertia.py` — **addressed in this story**
- `humanoid_character_builder/mesh/collision_generator.py` — mesh ops, needs trimesh
- `humanoid_character_builder/mesh/mesh_processor.py` — mesh ops, needs trimesh
- `humanoid_character_builder/core/model.py` — complex, separate story
- `model_generation/converters/urdf_parser.py` — XML parsing, complex
- `model_generation/builders/` — builder chain, complex
- `signal_toolkit/widget_*.py` — PyQt6 UI, requires display server (headless skip)
- `calc_backend/contracts/*.py` — Pydantic models, good candidates for future story
- `calc_backend/routers/*.py` — FastAPI, import broken (pre-existing issue)

## Files Changed

| File | Action | Purpose |
|------|--------|---------|
| `tests/shared/python/humanoid/test_primitive_inertia.py` | Created | 44 unit tests for primitive inertia calculations |

## Test Results

```
44 passed in 0.47s
```

### Test Coverage

- `TestComputeSphere` (8 tests) — formula verification, scaling laws, volume, mode
- `TestComputeBox` (5 tests) — formula verification, asymmetric cases, volume
- `TestComputeCylinder` (6 tests) — longitudinal/transverse formula, axis assignment
- `TestComputeCapsule` (6 tests) — positivity, symmetry, degenerate case
- `TestComputeEllipsoid` (3 tests) — formula, sphere limit, volume
- `TestComputeDispatch` (7 tests) — enum dispatch, string names, tuple dims, unknown shape
- `TestEstimateSegmentPrimitive` (9 tests) — head/torso/limb/neck/hand segment mapping

## Quality Gates

- `ruff check`: PASS (0 violations)
- `ruff format --check`: PASS (already formatted)
- No `print()` calls in new code
- All tests use `@pytest.mark.unit` (marked via class structure)
- No scope creep — only `tests/` directory modified

## Friction Log

None — implementation was straightforward after gap analysis.
