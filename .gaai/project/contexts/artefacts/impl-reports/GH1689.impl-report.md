---
id: GH1689
type: impl-report
created_at: 2026-03-22
---

# GH1689 Implementation Report

## Summary

- Removed conflicting `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.ruff.lint.per-file-ignores]` sections from `pyproject.toml` — `ruff.toml` is now the single authoritative ruff config
- Removed dead `[tool.black]` section from `pyproject.toml`
- Added `T201` to `[lint] select` in `ruff.toml` to enforce no-print policy in src/
- Added per-file-ignores for T201 in notebooks and root-level CLI scripts
- Updated `TestRuffT201Configured` tests to read from `ruff.toml` instead of `pyproject.toml`

## Changes Delivered

| File | Purpose |
|------|---------|
| `pyproject.toml` | Remove [tool.ruff], [tool.ruff.lint], [tool.ruff.lint.per-file-ignores], [tool.black] |
| `ruff.toml` | Add T201 to select; add **/*.ipynb and *.py to per-file-ignores |
| `tests/test_gh1655_print_to_logging.py` | Update TestRuffT201Configured to read ruff.toml |

## Verification

- `ruff check .` → All checks passed
- `ruff --show-settings` confirms py311 (from ruff.toml, not the removed py310 pyproject.toml setting)
- T201 now enforced in src/; suppressed for scripts/notebooks as appropriate
- 3/3 relevant tests pass; 3 pre-existing TestModernRoboticsLogging failures not introduced by this change
