---
id: GH1735
story: .gaai/project/contexts/artefacts/stories/GH1735.story.md
plan: .gaai/project/contexts/artefacts/plans/GH1735.execution-plan.md
verdict: PASS
created_at: 2026-04-20
---

# GH1735 Implementation Report

## Summary

Eliminated three classes of DRY violations in `src/pendulum_simulator`:

1. **Extracted `_make_det_evaluator` / `_make_cond_evaluator` factory functions**
   in `analysis_tab.py` — replaced 6 inlined closures (2 per model × 3 models)
   with 2 factory functions.

2. **Extracted `_assert_jac_keys_match` helper** in `test_analytical_jacobians.py`
   — replaced 3 identical `for key in (...): np.testing.assert_allclose(...)` blocks.

3. **Extracted `decomposition_result` fixture** in `test_club_forces.py` —
   replaced 3 identical `q/qdot/qddot/forces/result` setup blocks with a single
   shared fixture.

4. **Added unit tests** for `_make_det_evaluator` and `_make_cond_evaluator` in
   `test_analysis_tab.py` (`TestMakeDetEvaluator` + `TestMakeCondEvaluator`).

## Files Changed

| File | Change |
|------|--------|
| `src/pendulum_simulator/src/double_pendulum_golf/gui/analysis_tab.py` | Added `_make_det_evaluator`, `_make_cond_evaluator`; simplified 3 model evaluators |
| `src/pendulum_simulator/tests/test_club_forces.py` | Extracted `decomposition_result` fixture in `TestClubForceDecomposition` |
| `src/pendulum_simulator/tests/test_analytical_jacobians.py` | Extracted `_assert_jac_keys_match` helper; updated 3 test methods |
| `src/pendulum_simulator/tests/test_analysis_tab.py` | Added `TestMakeDetEvaluator` + `TestMakeCondEvaluator` test classes |

## AC Mapping

| AC | Status | Evidence |
|----|--------|---------|
| AC1 — DRY violations resolved | PASS | 3 classes of repeated blocks eliminated |
| AC2 — New code has tests | PASS | 6 new tests in `TestMakeDetEvaluator` + `TestMakeCondEvaluator` |
| AC3 — ruff check passes on modified files | PASS | `ruff check src/ tests/` → All checks passed |
| AC4 — No new print() in src/ | PASS | Verified via grep |
| AC5 — PR targeting staging | PASS | PR created |

## Rules Applied

- DRY: extracted repeated logic into named helpers/factories
- DbC: factory functions document preconditions in docstrings
- No print() in src/
- ruff format + check pass on all modified files
