---
id: GH1652
type: impl-report
status: complete
created_at: 2026-03-20
---

# GH1652 Implementation Report — Implement semantic versioning and deprecation path

## Summary

- Bumped `pyproject.toml` version from `0.3.0` to `1.0.0`, aligning the package
  metadata with the semantic-versioning baseline recorded in CHANGELOG [1.0.0].
- Bumped `programmatic_pid.__version__` from `0.3.0` to `1.0.0`.
- Added `src/shared/python/deprecation.py` — a production-quality `@deprecated`
  decorator for the shared library with `reason`, `removal_version`, DbC input
  validation, and `functools.wraps` metadata preservation.
- 14 unit tests in `tests/shared/python/test_deprecation.py`; all pass.

## Files Changed

| File | Change |
|------|--------|
| `pyproject.toml` | `version = "0.3.0"` → `version = "1.0.0"` |
| `CHANGELOG.md` | [Unreleased] section updated with versioning changes |
| `src/shared/python/deprecation.py` | NEW — shared `@deprecated` decorator |
| `src/shared/python/programmatic_pid/__init__.py` | `__version__` 0.3.0 → 1.0.0 |
| `tests/shared/python/test_deprecation.py` | NEW — 14 unit tests |

## Test Results

- 14 / 14 tests pass
- `ruff check` — zero violations
- `ruff format --check` — zero diffs
- No `print()` calls introduced in `src/`

## AC Coverage

| AC | Status |
|----|--------|
| AC1: Issue resolved (version bumped, @deprecated decorator added) | PASS |
| AC2: All new code has tests | PASS |
| AC3: ruff check passes | PASS |
| AC4: No new print() in src/ | PASS |
| AC5: PR targeting staging | PENDING (next step) |
