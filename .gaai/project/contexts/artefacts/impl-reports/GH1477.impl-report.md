---
id: GH1477
story: .gaai/project/contexts/artefacts/stories/GH1477.story.md
plan: .gaai/project/contexts/artefacts/plans/GH1477.execution-plan.md
verdict: PASS
created_at: 2026-03-19
---

# GH1477: Implementation Report — Address TDD, DRY, DbC, and LOD in src/optimizer_gui

## Summary

Fixed all DbC and LoD violations in `src/optimizer_gui/python/optimizer_gui/ui/pyqt6/main_window.py` as identified in the adversarial assessment. Added 8 new tests covering the DbC preconditions.

## Files Modified

| File | Change |
|------|--------|
| `src/optimizer_gui/python/optimizer_gui/ui/pyqt6/main_window.py` | Replaced `assert`-based DbC with proper `raise`; extracted enum constants; introduced `_switch_to_results_tab()` helper |
| `src/optimizer_gui/tests/test_optimizer_gui.py` | Added `TestEvalRosenbrockDbC` (6 tests) and `TestInitAdamArraysDbC` (2 tests) |

## AC Mapping

| AC | Status | Evidence |
|----|--------|---------|
| AC1: Issue #1477 resolved | PASS | All 5 DbC violations and 8 LoD violations addressed |
| AC2: New code has tests | PASS | 8 new tests in TestEvalRosenbrockDbC + TestInitAdamArraysDbC |
| AC3: ruff check passes | PASS | `ruff check src/optimizer_gui/` — all checks passed |
| AC4: No new print() | PASS | grep confirms no print() in modified src files |
| AC5: PR targeting staging | PENDING | PR to be created by Orchestrator |

## DbC Fixes Detail

| Function | Before | After |
|----------|--------|-------|
| `_run_adam_demo` | `assert params is not None` | `isinstance` check + `TypeError`; empty check + `ValueError` |
| `_eval_rosenbrock` | `assert values is not None` | `isinstance(np.ndarray)` + `TypeError`; empty check + `ValueError` |
| `_compute_numerical_gradient` | `assert values is not None` | Full type checks on values/lower/upper; shape mismatch `ValueError` |
| `_display_adam_results` | `assert best_obj is not None` | Type checks on all 3 params; positive value check on max_iterations |
| `_run_surface_demo` | `assert params is not None` | `isinstance` check + `TypeError` for both params and method |

## LoD Fixes Detail

| Location | Violation | Fix |
|----------|-----------|-----|
| `_setup_ui` line ~257 | `Qt.ScrollBarPolicy.ScrollBarAlwaysOff` | Extracted to `no_hscroll` local var |
| `_setup_ui` line ~273 | `Qt.AlignmentFlag.AlignCenter` | Extracted to `align_center` local var |
| `_create_adam_settings_tab` line ~378 | `QSizePolicy.Policy.Expanding/Fixed` | Extracted to `size_expanding`, `size_fixed` |
| `_display_adam_results` + `_run_surface_demo` | `self.tab_widget.setCurrentIndex(2)` repeated | Extracted to `_switch_to_results_tab()` helper |

## Rules Applied

- DbC: `TypeError` for wrong types, `ValueError` for out-of-range (CLAUDE.md)
- No `print()` in src/ (CLAUDE.md)
- ruff format + ruff check clean

## Known Limitations

- New DbC tests skip when PyQt6 is unavailable (headless test environment). This is the existing pattern for all PyQt6-dependent tests in this module.
- 3 pre-existing test failures in `TestOptimizerGUIRegistration` (GUI_METADATA vs GUI_INFO mismatch, missing `main()` in launcher) are out of scope for this story.
