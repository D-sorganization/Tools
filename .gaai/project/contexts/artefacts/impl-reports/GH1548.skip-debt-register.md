---
id: GH1548
artefact_type: skip-debt-register
created_at: 2026-03-19
---

# GH1548 Skip-Debt Register: Shared-Package Tests

Generated as part of GH1548 audit and remediation.

---

## Skip Debt Eliminated

| File | Previous Pattern | Problem | Fix Applied |
|------|-----------------|---------|-------------|
| `tests/shared/python/chat/test_chat_dock_widget.py` | `TestSessionFileHelpers` gated behind module-level `importorskip("PyQt6.QtWidgets")` | Session-file helpers are pure Python; no Qt dependency at runtime | Extracted `TestSessionFileHelpers` to `test_chat_session_helpers.py` (only `importorskip("PyQt6")`, not `PyQt6.QtWidgets`) |
| `tests/shared/python/plot_theme/test_integration.py` | `pytest.mark.skipif(not pytest.importorskip("matplotlib", ...), ...)` | `importorskip` inside `skipif` condition is incorrect pattern — `importorskip` raises `Skipped` before `not` can evaluate | Replaced with module-level `_matplotlib_available()` helper + `_skip_no_matplotlib` mark |
| `tests/shared/python/plot_theme/test_manager.py` | Inline `pytest.importorskip("matplotlib")` inside test body | Non-idiomatic; hides skip intent from static analysis | Replaced with `@_skip_no_matplotlib` decorator using module-level `_matplotlib_available()` helper |

---

## Legitimate Optional Skips (retained)

| File | Skip Pattern | Dependency | Reason |
|------|-------------|-----------|--------|
| `tests/shared/python/utils/test_data_io.py` | `_skip_no_pyarrow` on parquet tests | pyarrow | Genuinely optional; CSV/TSV tests run unconditionally |
| `tests/shared/python/chat/test_models.py` | Module-level `importorskip("pydantic")` | pydantic | Optional dep; all models tests require it |
| `tests/shared/python/chat/test_chat_session_helpers.py` | Module-level `importorskip("PyQt6")` | PyQt6 | Module-level import in source requires PyQt6; documented, isolated |
| `tests/shared/python/chat/test_chat_dock_widget.py` | Module-level `importorskip("PyQt6.QtWidgets")` + `importorskip("pytestqt")` | PyQt6 + pytest-qt | Widget tests genuinely require a display server and pytest-qt |
| `tests/shared/python/notes/test_notes_dock_widget.py` | `importorskip("PyQt6.QtWidgets")` + `RUN_QT_TESTS=1` env gate | PyQt6 | Full widget lifecycle tests; `RUN_QT_TESTS` gate is correct for headless CI |
| `tests/shared/python/theme/test_theme_manager.py` | Module-level `importorskip("PyQt6")` | PyQt6 | ThemeManager is a PyQt6 class; entire suite requires Qt |
| `tests/shared/python/plot_theme/test_integration.py` | `@_skip_no_matplotlib` on two methods | matplotlib | Matplotlib-specific tests (`create_themed_figure`, `style_axis`); non-matplotlib tests run unconditionally |
| `tests/shared/python/plot_theme/test_manager.py` | `@_skip_no_matplotlib` on one method | matplotlib | `apply_to_matplotlib()` test; all other PlotThemeManager tests run unconditionally |

---

## Residual Debt (accepted, future work)

| Item | Reason for deferral | Future action |
|------|--------------------|----|
| Session helpers import from `chat_dock_widget.py` which has module-level PyQt6 imports | Refactoring `chat_dock_widget.py` to expose a `chat.session_helpers` pure module is out of scope for this story | Tracked separately: extract pure-Python helpers to avoid PyQt6 import dependency entirely |
| PyQt6 widget tests skip on headless CI | Requires display server or virtual display (xvfb) setup in CI | Evaluate xvfb or offscreen Qt backend for CI; out of scope here |

---

## Verification

After applying fixes, the following tests run without skip on this machine:

```
tests/shared/python/chat/test_chat_session_helpers.py  5 passed
tests/shared/python/plot_theme/test_integration.py    11 passed (2 would skip without matplotlib)
tests/shared/python/plot_theme/test_manager.py        17 passed (1 would skip without matplotlib)
```
