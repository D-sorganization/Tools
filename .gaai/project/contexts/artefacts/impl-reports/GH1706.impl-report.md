---
id: GH1706
type: impl-report
story: contexts/artefacts/stories/GH1706.story.md
plan: contexts/artefacts/plans/GH1706.execution-plan.md
created_at: 2026-03-22
verdict: IMPLEMENTED
---

# GH1706 Implementation Report

## Summary

- Changed CI module size budget from `--max-lines 1500` to `--max-lines 1200` in `.github/workflows/ci-standard.yml` — aligning enforcement with the documented 1200-line standard.
- Changed `DEFAULT_MAX_LINES` from 1500 to 1200 in `scripts/check_module_size_budget.py` — keeping script default consistent with CI invocation.
- Added `src/rotation_converter/ui/pyqt6/main_window.py` (1,233 lines) to `config/module_size_budget_baseline.json` — the only file in the 1200-1500 gap zone; it is a PyQt6 GUI monolith and a known exception.

## Changes Made

| File | Change | AC |
|------|--------|----|
| `.github/workflows/ci-standard.yml` | `--max-lines 1500` → `--max-lines 1200` on line 135 | AC1 |
| `scripts/check_module_size_budget.py` | `DEFAULT_MAX_LINES = 1500` → `DEFAULT_MAX_LINES = 1200` | AC1 |
| `config/module_size_budget_baseline.json` | Added `"src/rotation_converter/ui/pyqt6/main_window.py": 1233` | AC1 |

## Acceptance Criterion Mapping

| AC | Status | Evidence |
|----|--------|---------|
| AC1: Issue resolved | DONE | CI now enforces 1200-line limit; discrepancy between docs and CI eliminated |
| AC2: All new code has tests | N/A | No new Python code written; only CI config + JSON updated |
| AC3: ruff check passes on modified files | DONE | `ruff check scripts/check_module_size_budget.py` — 0 violations |
| AC4: No new print() in src/ | DONE | No src/ files modified; script uses sys.stdout.write/sys.stderr.write |
| AC5: PR created targeting staging | PENDING | To be created by Orchestrator after QA PASS |

## Rules Applied

- DbC: No public API changed
- JSON validated: `python3 -c "import json; json.load(open('config/module_size_budget_baseline.json'))"` — valid, 10 entries
- Ruff: 0 violations on modified Python file
- Script functionality verified: `python3 scripts/check_module_size_budget.py --max-lines 1200 --include src` exits 0

## Known Risks / Limitations

- GH1690 (story/GH1690 branch, not yet merged) also adds `main_window.py` to the baseline. When both PRs merge, the baseline entry will be a trivial duplicate — easily resolved.
- The DEFAULT_MAX_LINES change in the script only affects invocations without `--max-lines` flag. CI always passes it explicitly, so the default is a documentation/consistency benefit rather than a functional change.

## Specialists Invoked

None.
