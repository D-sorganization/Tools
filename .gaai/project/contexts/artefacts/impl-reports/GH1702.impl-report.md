---
id: GH1702
type: impl-report
created_at: 2026-03-22
---

# GH1702 Implementation Report

## Summary

- Removed the global `"**/*.py" = ["E501"]` per-file-ignore from `ruff.toml` that was
  silently suppressing all line-length violations across the entire Python codebase.
- Added targeted `# noqa: E501` comments to 4 specific lines in 3 files where the
  content genuinely cannot be shortened (module docstrings, HTML strings, markdown strings).
- `ruff check` passes on all modified files.

## Changes Delivered

| File | Change |
|------|--------|
| `ruff.toml` | Removed `"**/*.py" = ["E501"]` from `[lint.per-file-ignores]` |
| `src/folder_tool/folder_tool_ui_processing.py` | Added `# noqa: E501` to module docstring (line 1, 90 chars) |
| `src/shared/python/upstream_drift_tools/process_calculators/psa_package/psa_gui.py` | Added `# noqa: E501` to HTML string (line 928) and docstring (line 994) |
| `src/shared/python/upstream_drift_tools/process_calculators/psa_package/psa_webapp.py` | Added `# noqa: E501` to markdown string (line 681) |

## Pre-Existing Violations Discovered

The global suppression was hiding 438 E501 violations across 115+ files in `src/`
and additional violations in `tests/` and `scripts/`. These are pre-existing and
out of scope for this story. A follow-up story could address them systematically.

## Acceptance Criteria

| AC | Status | Notes |
|----|--------|-------|
| AC1: Issue resolved | PASS | Global E501 suppression removed |
| AC2: TDD | N/A | No new code |
| AC3: ruff check on modified files | PASS | All checks passed |
| AC4: No new print() in src/ | PASS | None added |
| AC5: PR created | PENDING | Next step |
