---
id: GH1489
story: .gaai/project/contexts/artefacts/stories/GH1489.story.md
created_at: 2026-03-19
status: complete
---

# GH1489 Implementation Report

## Summary

- Fixed 6 LoD violations by extracting 3-level Qt enum chains to module-level aliases
- Fixed 3 DbC violations by replacing weak `assert` statements with explicit `ValueError`/`TypeError` guards
- Added `TestSyngasWaterCalculatorDbCLoD` class with 6 new tests covering all preconditions and alias existence

## Files Modified

| File | Purpose |
|------|---------|
| `src/syngas_water_calculator/python/syngas_water_calculator/ui/pyqt6/main_window.py` | Add LoD aliases, fix DbC preconditions |
| `src/syngas_water_calculator/tests/test_syngas_water_calculator_gui.py` | Add 6 new DbC/LoD tests |

## Changes Delivered

### main_window.py

**LoD fixes (module-level aliases added after imports):**
```python
_SCROLL_ALWAYS_OFF = Qt.ScrollBarPolicy.ScrollBarAlwaysOff
_ALIGN_CENTER = Qt.AlignmentFlag.AlignCenter
_SIZE_EXPANDING = QSizePolicy.Policy.Expanding
_SIZE_FIXED = QSizePolicy.Policy.Fixed
```
- `Qt.ScrollBarPolicy.ScrollBarAlwaysOff` → `_SCROLL_ALWAYS_OFF` (line 186)
- `Qt.AlignmentFlag.AlignCenter` → `_ALIGN_CENTER` (line 202)
- `QSizePolicy.Policy.Expanding/Fixed` → `_SIZE_EXPANDING/_SIZE_FIXED` (lines 235, 246)

**DbC fixes:**
- `_display_water_content_results`: added `if result is None: raise ValueError("result must not be None")`
- `_display_risk_assessment`: replaced `assert risk is not None` with `ValueError` (None) + `TypeError` (non-dict)
- `_display_calculation_error`: replaced `assert error is not None` with `ValueError` (None) + `TypeError` (non-Exception)

### test_syngas_water_calculator_gui.py

Added `TestSyngasWaterCalculatorDbCLoD` with 6 tests:
1. `test_display_water_content_results_raises_value_error_for_none`
2. `test_display_risk_assessment_raises_value_error_for_none`
3. `test_display_risk_assessment_raises_type_error_for_non_dict`
4. `test_display_calculation_error_raises_value_error_for_none`
5. `test_display_calculation_error_raises_type_error_for_non_exception`
6. `test_module_level_aliases_exist`

Test infrastructure uses `importlib.util.spec_from_file_location` to load the module under mocked PyQt6 with a `_FakeBaseCalculatorWindow` stub to allow proper attribute assignment.

## Test Results

- 6 new tests: PASS
- 12 pre-existing tests: SKIP (environment lacks PyQt6/syngas_water_calculator installed package)
- 2 pre-existing failures (gui_registration.GUI_METADATA missing): pre-existing, not introduced by this story
- `ruff check`: zero violations
- `ruff format --check`: zero diffs
- No new print() calls in src/
