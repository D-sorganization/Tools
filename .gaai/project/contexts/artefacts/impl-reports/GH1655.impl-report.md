---
id: GH1655
type: impl-report
status: complete
created_at: 2026-03-20
---

# GH1655 Implementation Report

## Summary

- Replaced all unguarded `print()` calls in library source files with `logging`
- Added T201 ruff rule to enforce no-print policy going forward
- Jupyter notebooks excluded from T201 (legitimate use of print for output)
- 6 new TDD tests covering logging migration and ruff configuration

## Changes Delivered

| File | Purpose |
|------|---------|
| `src/rotation_converter/modern_robotics.py` | Added `logging` import, `_logger = logging.getLogger(__name__)`, replaced 2 print() docstring examples with _logger.warning() |
| `src/pendulum_simulator/tests/test_analytical_jacobians.py` | Added `logging` import, `_logger`, replaced 9 benchmark-timing print() calls with _logger.debug() |
| `src/pendulum_simulator/test_sim.py` | Added `# noqa: T201` to 7 print() calls (legitimate CLI script output) |
| `pyproject.toml` | Added `T201` to `[tool.ruff.lint] select`; added `T201` to `**/*.ipynb` per-file-ignores |
| `tests/test_gh1655_print_to_logging.py` | New: 6 TDD tests for logging migration and T201 configuration |

## Acceptance Criteria Status

| AC | Status | Notes |
|----|--------|-------|
| AC1: Issue resolved | PASS | All print() calls migrated or suppressed |
| AC2: All new code has tests | PASS | 6 tests in test_gh1655_print_to_logging.py |
| AC3: ruff check passes | PASS | Zero violations with T201 enabled |
| AC4: No new print() in src/ | PASS | T201 now enforced + grep verified |
| AC5: PR to staging | PENDING | PR creation step |
