---
id: GH1498
story: .gaai/project/contexts/artefacts/stories/GH1498.story.md
plan: .gaai/project/contexts/artefacts/plans/GH1498.execution-plan.md
created_at: 2026-03-19
---

# GH1498 Implementation Report

## Summary of Changes

| File | Change | AC |
|------|--------|----|
| `tests/conftest.py` | Replaced 8 `mock.method.return_value = X` patterns with `configure_mock(**{...})` in `mock_file`, `mock_response`, and `mock_path` fixtures | AC1 |
| `tests/helpers/numerical.py` | Added `isinstance` precondition guards to `assert_monotonic` for `values`, `increasing`, `strict` args | AC1, AC2 |
| `tests/helpers/test_numerical.py` | Added 3 TDD-first DbC tests: `test_rejects_non_list_values`, `test_rejects_non_bool_increasing`, `test_rejects_non_bool_strict` | AC2 |
| `tests/tools/folder_tools/conftest.py` | Broke 4-level `Path(__file__).resolve().parents[3]` chain into `_this_file` + `_repo_root` intermediate vars | AC1 |

## Acceptance Criterion Mapping

- **AC1** (issue resolved): All LoD violations (deep attribute chains) eliminated. DbC preconditions added to `assert_monotonic`. No src/ changes needed.
- **AC2** (all new code has tests): 3 new tests added to `test_numerical.py` before implementation (TDD). All 24 tests pass.
- **AC3** (`ruff check` passes): Verified clean on all 4 modified files.
- **AC4** (no new `print()` in `src/`): No `src/` files modified.
- **AC5** (PR created): PR to follow.

## Rules Applied

- LOD: `configure_mock(**{...})` collapses multi-level mock chains to single Python attribute access
- DbC: `isinstance` guards with `TypeError` for wrong types match project DbC standard
- TDD: test file updated before implementation changes committed

## Known Risks / Limitations

None. All changes are contained to test infrastructure. No src/ public API changes.
