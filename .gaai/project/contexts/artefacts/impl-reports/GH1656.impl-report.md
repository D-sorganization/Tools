---
id: GH1656
story: .gaai/project/contexts/artefacts/stories/GH1656.story.md
created_at: 2026-03-20
verdict: PASS
---

# GH1656 Implementation Report: Add ODE Solver Timeout and Load Testing

## Summary

- Added cross-platform `with_timeout()` decorator in new `ode_solver/timeout.py`
- Integrated timeout into `main_window.py` `_solve()` — ODE computations now bounded to 30 s
- Added 16 tests (12 unit + 4 load/performance) in `tests/ode_solver/test_ode_solver_timeout.py`

## Changes Delivered

| File | Purpose |
|------|---------|
| `src/ode_solver/python/ode_solver/timeout.py` | New — `with_timeout()` decorator + `SolverTimeoutError` |
| `src/ode_solver/python/ode_solver/ui/pyqt6/main_window.py` | Modified — timeout integration in `_solve()`, `_log` logger |
| `tests/ode_solver/test_ode_solver_timeout.py` | New — unit + load performance tests |

## Acceptance Criteria Results

| AC | Status | Notes |
|----|--------|-------|
| AC1: Issue #1656 resolved | PASS | Timeout added; unconstrained solve no longer possible |
| AC2: All new code has tests | PASS | 16 tests cover `timeout.py` fully; load tests skip if upstream unavailable |
| AC3: `ruff check` passes | PASS | Zero violations on all 3 files |
| AC4: No new `print()` in `src/` | PASS | Uses `logging.warning()` via `_log` |
| AC5: PR targeting staging | PENDING | PR to be created |

## Technical Notes

- Thread-based timeout is cross-platform (no `signal.SIGALRM`); compatible with pytest-xdist `-n auto`
- Abandoned threads are daemon threads — no resource leak
- `_SOLVER_TIMEOUT_S = 30.0` is a class-level constant for easy override/testing
- Performance tests marked `@pytest.mark.slow @pytest.mark.performance`; skip if upstream_drift_tools unavailable

## Test Results

- 25/25 ode_solver tests pass (13 existing + 12 new unit)
- 4 load/performance tests deselected in standard run (marked `slow`)
- ruff check: 0 violations
- ruff format: 0 diffs
