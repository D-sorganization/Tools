---
id: GH1707
type: memory-delta
story: contexts/artefacts/stories/GH1707.story.md
created_at: 2026-03-22
delta_type: annotation-coverage
---

# GH1707 Memory Delta

## Summary

Type annotation backfill for `src/shared/python/`. No architectural decisions or new patterns introduced.

## Coverage Change

- Before: 24 unannotated functions across 22 files in `src/shared/python/`
- After: 0 unannotated functions

## Notable Pattern Established

When annotating `__init__` methods that were previously unannotated in files that are excluded from mypy CI (e.g., `model_generation/`, `humanoid_character_builder/`), the convention is:
- `__init__` → `-> None`
- Always-raises stubs → `-> NoReturn`
- Functions returning a class object → `-> type[ClassName]` with `TYPE_CHECKING` guard

## Recommendations for Discovery

- The 9 pre-existing ruff format violations on staging (in non-shared files) could be a separate assessment item.
- The `disallow_untyped_defs = True` in `mypy.ini` is already global. The mypy.ini exclusions for `model_generation/`, `humanoid_character_builder/`, and `upstream_drift_tools/` are the actual coverage gap. A future story could remove those exclusions as annotation coverage improves.
