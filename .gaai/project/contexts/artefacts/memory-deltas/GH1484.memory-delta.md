---
id: GH1484
story: GH1484
created_at: 2026-03-19
delta_type: pattern-reinforcement
---

# GH1484 Memory Delta

## Pattern Reinforced

**DbC assert→raise migration pattern** (same as GH1488, GH1489, GH1493, etc.)

All `assert x is not None, "msg"` preconditions in `src/rrt_path_planner/python/src/star_wars_rrt.py`
were replaced with `if x is None: raise TypeError(...)`. For typed arguments (`int`, `str`),
`isinstance` checks were used instead.

No new decisions. Pattern is consistent with GH1488/GH1489/GH1493 deliveries.

## Path Reference Fix

`launch_rrt_asteroid_navigator.py` referenced `src/scientific_modeling/rrt_path_planner/`
which no longer exists post-flattening (commit bf7dd8c6). The LoD fix (intermediate vars)
simultaneously corrected the path to `src/rrt_path_planner/`. Discovery may want to audit
other launcher scripts for stale `scientific_modeling` path references.
