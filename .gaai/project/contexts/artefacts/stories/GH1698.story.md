---
id: GH1698
github_issue: 1698
repo: D-sorganization/Tools
title: "Assessment: 304 noqa suppressions in src/ — audit for stale entries"
created_at: 2026-03-22T21:21:13Z
---

# GH1698: Assessment: 304 noqa suppressions in src/ — audit for stale entries

**Source:** D-sorganization/Tools#1698

## Description

## Assessment Category: N — Noise (Suppression Audit)

**Severity:** Low — Suppressions are a normal part of development but need periodic review.

### Finding

- **304 `# noqa` comments** in src/
- **9 `# nosec` comments** in src/
- **1,137 `# type: ignore[...]`** (targeted) in src/
- **4 bare `# type: ignore`** (untargeted) — good that this is low

The `noqa` and `nosec` counts are reasonable for a 264K-line codebase, but many may be stale after refactoring.

### Recommendation

Run `ruff check --extend-select ALL` on a sample of files with `noqa` to verify the suppressed violations still exist. Remove stale suppressions.

## Acceptance Criteria

1. The issue described in #1698 is resolved
2. All new code has tests (TDD)
3. `ruff check` passes on modified files
4. No new `print()` calls in `src/`
5. PR created targeting `staging` branch with clear description
