---
id: GH1546
github_issue: 1546
repo: D-sorganization/Tools
title: "Add cross-repo Python consumer integration workflow for Gasification_Model and UpstreamDrift"
created_at: 2026-03-19T13:41:55Z
---

# GH1546: Add cross-repo Python consumer integration workflow for Gasification_Model and UpstreamDrift

**Source:** D-sorganization/Tools#1546

## Description

## Problem

Rust downstream integration is enforced, but shared Python packages are not validated against downstream consumers with the same rigor.

## Required Changes

- Add a required GitHub Actions workflow that checks out downstream consumers
- Validate install/bootstrap of the current `Tools` branch into:
  - `Gasification_Model`
  - `UpstreamDrift`
- Run consumer-contract tests that prove real imports and basic runtime behavior
- Fail on import-path drift, missing package exports, or broken bootstrap assumptions

## Acceptance Criteria

- CI fails when current `Tools` changes break either downstream Python consumer
- Workflow output identifies the failing consumer and contract category
- This job is required for merges affecting shared packages

## Acceptance Criteria

1. The issue described in #1546 is resolved
2. All new code has tests (TDD)
3. `ruff check` passes on modified files
4. No new `print()` calls in `src/`
5. PR created targeting `staging` branch with clear description
