---
id: GH1699
github_issue: 1699
repo: D-sorganization/Tools
title: "Assessment: Cross-repo integration tests pass but lack Gasification_Model coverage"
created_at: 2026-03-22T21:16:25Z
---

# GH1699: Assessment: Cross-repo integration tests pass but lack Gasification_Model coverage

**Source:** D-sorganization/Tools#1699

## Description

## Assessment Category: O — Orthogonality (Cross-Repo Integration)

**Severity:** Medium — Integration tests exist and pass for UpstreamDrift patterns, but Gasification_Model patterns are not tested.

### Finding

**Cross-repo tests: PASS (38/38).** `tests/test_cross_repo_import_compatibility.py` validates:
- upstream_drift_tools top-level namespace
- Protocol interfaces (Calculator, ProcessCalculator, DataTransformer, etc.)
- CalculationResult and ValidationResult data classes
- InputValidator static methods
- Calculators and data_processing subpackages

**Gaps:**
1. No Gasification_Model-specific import patterns tested (only UpstreamDrift patterns)
2. `tests/heavy_integration/` has 3 failures due to missing optional deps (trimesh, ezdxf) — these should use `pytest.importorskip`
3. The `cross-repo-python-integration.yml` workflow exists but its actual behavior was not verified during this assessment
4. No tests verify that changes to `contracts.py` don't break downstream DbC usage

### Related

- #1653 (add cross-repo integration test)
- #1546 (cross-repo consumer integration workflow)

## Acceptance Criteria

1. The issue described in #1699 is resolved
2. All new code has tests (TDD)
3. `ruff check` passes on modified files
4. No new `print()` calls in `src/`
5. PR created targeting `staging` branch with clear description
