---
id: GH1601
github_issue: 1601
repo: D-sorganization/Tools
title: "type: 739 type: ignore comments + legacy directories excluded from mypy"
created_at: 2026-03-19T13:41:54Z
---

# GH1601: type: 739 type: ignore comments + legacy directories excluded from mypy

**Source:** D-sorganization/Tools#1601

## Description

## Problem
739 `# type: ignore` comments exist across the codebase. Multiple legacy directories are explicitly excluded from mypy type checking: `signal_toolkit`, `model_generation`, `programmatic_pid`, `data_processing`, `electrode_advisor`. These exclusions mean runtime type errors in these modules won't be caught.

## Fix (incremental)
1. Run `mypy src/upstream_drift_tools/calculators/` — most actively maintained, should be fixable first
2. Resolve type: ignore in process calculators (highest value for consumers)
3. Gradually re-enable mypy for excluded directories

## Acceptance Criteria
- [ ] type: ignore count reduced by at least 50% in core calculator modules
- [ ] At least one previously-excluded directory re-enabled for mypy

## Acceptance Criteria

1. The issue described in #1601 is resolved
2. All new code has tests (TDD)
3. `ruff check` passes on modified files
4. No new `print()` calls in `src/`
5. PR created targeting `staging` branch with clear description
