---
id: GH1685
type: execution-plan
tier: 1
created_at: 2026-03-22
---

# GH1685 Execution Plan

## Story
Assessment: 25 files fail ruff format check on staging

## Tier
1 (MicroDelivery) — single mechanical command, no logic changes

## Approach
Run `ruff format .` in the repository root. This is the established convention per CLAUDE.md.
No approach evaluation required — convention is explicit.

## Steps

1. Run `ruff format --check .` to confirm 25 files need reformatting
2. Run `ruff format .` to apply formatting
3. Verify `ruff format --check .` reports 0 files
4. Verify `ruff check .` passes clean
5. Verify no new `print()` calls in `src/` via git diff
6. Run tests on reformatted test files
7. Commit to story branch
8. Write delivery artefacts and commit
9. Create PR targeting staging

## Risk Register

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| Test failures from formatting | Low | Formatting is purely cosmetic; no logic changed |
| Ruff lint issues introduced | None | ruff format only changes whitespace/style |

## Rollback Boundary
Single commit — fully revertible.
