---
id: GH1485
story: .gaai/project/contexts/artefacts/stories/GH1485.story.md
tier: 2
created_at: 2026-03-19
repo: Tools_Private
---

# GH1485 Execution Plan

## Story Summary
Adversarial Assessment: Address TDD, DRY, DbC, and LOD in `src/scrubber_calculator`

## Pre-flight Assessment

The code in `src/scrubber_calculator/python/scrubber_calculator/ui/pyqt6/main_window.py`
has been partially addressed in prior work. Current state:

### DbC Violations
- `ResultCard.__init__` at line 59 already has `assert title is not None, "title must be provided"` — resolved.
- `ScrubberCalculatorWindow.__init__` takes no typed arguments — no precondition needed.

### LoD Violations
All cited violations are resolved in the current file:
- Lines 420-423: `results_header = self.results_table.horizontalHeader()` with `if results_header is not None:` guard — correct LoD pattern.
- Lines 444-446: `header = self.details_table.horizontalHeader()` with `if header is not None:` guard — correct LoD pattern.
- No 3+-chain attribute accesses remain at the cited lines.

### Test Infrastructure Gap
The `src/scrubber_calculator/tests/` directory is missing:
1. A `conftest.py` that adds `python/` to sys.path (so `scrubber_calculator` package is importable).
2. The existing `test_scrubber_calculator_gui.py` imports `upstream_drift_tools.bootstrap` at module level without try/except, causing test collection failure.
3. The test assertion `"QMainWindow" in stylesheet` in `test_main_window_creation` is wrong — `get_stylesheet()` does not emit "QMainWindow" text.

`ruff check` already passes on all affected files. No `print()` in src/.

## Complexity Assessment
- Tier: 2 (Core Team)
- Complexity: 3
- Files affected: `src/scrubber_calculator/tests/` (conftest.py new, test file fix)
- Risk: Low — test infrastructure fix, no API or logic changes

## Implementation Steps

### Step 1: Create conftest.py for scrubber_calculator/tests
Create `src/scrubber_calculator/tests/conftest.py` following the established pattern
from `electrode_advisor/tests/conftest.py` and `urdf_builder_gui/tests/conftest.py`:
- Add `python/` subdirectory to sys.path so `scrubber_calculator` package is importable
- Add `src/shared/python` (relative to src root) for upstream_drift_tools

### Step 2: Fix broken import in test file
In `test_scrubber_calculator_gui.py`, wrap the `upstream_drift_tools.bootstrap` import
in a try/except ImportError block following the `glass_bath_fea` pattern.

### Step 3: Fix wrong test assertion
`test_main_window_creation` asserts `"QMainWindow" in stylesheet`. The `get_stylesheet()`
function returns a string with `QFrame#resultCard` content and base stylesheet content
but NOT "QMainWindow". Fix assertion to check for actual content (e.g., `QFrame#resultCard`
or part of the Catppuccin base stylesheet).

### Step 4: Verify tests collect and pass
Run `python3 -m pytest src/scrubber_calculator/tests/ -v --timeout=30` and confirm
all tests pass.

### Step 5: Verify ruff compliance
Run `ruff check src/scrubber_calculator/` and `ruff format --check src/scrubber_calculator/`
to confirm zero violations.

## Acceptance Criteria Mapping
1. AC1: Issue resolved — DbC and LoD violations verified already addressed ✓
2. AC2: All new code has tests (TDD) — conftest.py is infrastructure, test assertions corrected
3. AC3: `ruff check` passes — confirmed passing, will verify post-change
4. AC4: No new `print()` in `src/` — no new code adds print()
5. AC5: PR created targeting `staging` branch — pending

## Files Expected to Change
- `src/scrubber_calculator/tests/conftest.py` — NEW (test path setup)
- `src/scrubber_calculator/tests/test_scrubber_calculator_gui.py` — MODIFY (fix import pattern, fix assertion)

## Risk Register
- **Low**: Patching the test file import pattern is low-risk, well-established convention
- **Low**: The conftest.py follows exact same pattern as electrode_advisor and urdf_builder_gui

## Rollback Boundaries
- All changes in `src/scrubber_calculator/tests/` only
- No production code changes
- No API changes
