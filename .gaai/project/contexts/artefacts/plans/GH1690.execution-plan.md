---
id: GH1690
type: execution-plan
story: contexts/artefacts/stories/GH1690.story.md
created_at: 2026-03-22
---

# GH1690 Execution Plan: Assessment — 2 files exceed 1200-line budget

## Summary

Two files exceed the 1200-line budget:
1. `src/rotation_converter/modern_robotics.py` — 2,084 lines — **already baselined** in `config/module_size_budget_baseline.json`
2. `src/rotation_converter/ui/pyqt6/main_window.py` — 1,233 lines — **NOT baselined**

The resolution is to add `src/rotation_converter/ui/pyqt6/main_window.py` to the baseline as a known exception (PyQt6 GUI file; monolithic UI structure is an accepted trade-off until a dedicated refactor story targets it). This is the minimal, reversible resolution called out in the story recommendation.

No new code is being written. The only change is a JSON file addition.

## Tier: 2 (Core Team)

## Approach Evaluation

- **Option A: Baseline the file** — Add to `config/module_size_budget_baseline.json`. Resolves AC1. No new code required (AC2 vacuously satisfied). Low risk. Reversible.
- **Option B: Split main_window.py** — Significant refactor of a 1,233-line PyQt6 GUI. Requires new tests. High risk of regression in UI logic. Scope not justified for an "Assessment" story.

**Chosen: Option A** — baselining is the recommendation-aligned, minimal-risk resolution for an assessment-class story.

## Implementation Steps

### Step 1: Add main_window.py to baseline JSON

**File:** `config/module_size_budget_baseline.json`

Add entry:
```json
"src/rotation_converter/ui/pyqt6/main_window.py": 1233
```

**Edge cases:**
- Confirm exact current line count matches (wc -l confirms 1233)
- JSON must remain valid after edit

### Step 2: Verify no ruff violations

Run `python3 -m ruff check config/module_size_budget_baseline.json` — JSON file, ruff skips non-Python files. No violations expected.

### Step 3: Verify no print() calls introduced

No Python source modified. AC4 vacuously satisfied.

### Step 4: Commit

Atomic commit in worktree on story/GH1690 branch.

## Test Checkpoints

- AC1: `src/rotation_converter/ui/pyqt6/main_window.py` entry present in baseline JSON with value 1233
- AC2: No new code written — vacuously satisfied
- AC3: `ruff check` on config/ passes (JSON not linted by ruff)
- AC4: No print() in src/ introduced — no src/ files modified
- AC5: PR created targeting staging

## Risk Register

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| JSON syntax error in edit | Low | Validate JSON after edit |
| Wrong line count baselined | Low | Confirm with wc -l before writing |

## Rollback

Revert the baseline JSON entry. No code changed, so no downstream impact.

## Files Affected

| File | Change |
|------|--------|
| `config/module_size_budget_baseline.json` | Add 1 entry for main_window.py |
