---
id: GH1689
type: execution-plan
tier: 1
created_at: 2026-03-22
---

# GH1689 Execution Plan: Consolidate Dual Ruff Config

## Tier Assessment

Tier 1 (MicroDelivery) — complexity ≤ 2, files_affected ≤ 3, criteria_count = 5, no specialist triggers.

## Problem

Two ruff configs exist with conflicting settings:
- `ruff.toml` (authoritative, py311, selects E/F/W/I/B/UP, missing T201)
- `pyproject.toml [tool.ruff]` (overridden, py310, selects E/F/B/UP/I/T201)
- `pyproject.toml [tool.black]` (dead config, repo uses ruff format)

## Approach

1. Remove `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.ruff.lint.per-file-ignores]` from `pyproject.toml`
2. Remove `[tool.black]` from `pyproject.toml`
3. Add T201 to `ruff.toml` select (was missing, present in dead pyproject.toml config)
4. Add `**/*.ipynb` per-file-ignore for T201 in `ruff.toml`
5. Add `*.py` per-file-ignore for T201 in `ruff.toml` (root-level CLI scripts)
6. Update `tests/test_gh1655_print_to_logging.py` to read from `ruff.toml` (was reading pyproject.toml)

## Files Affected

- `pyproject.toml` — remove conflicting [tool.ruff] and [tool.black]
- `ruff.toml` — add T201 to select, add notebook and root-script per-file-ignores
- `tests/test_gh1655_print_to_logging.py` — update TestRuffT201Configured to read ruff.toml
