---
id: GH1688
type: execution-plan
tier: 1
created_at: 2026-03-22
---

# GH1688 Execution Plan

## Tier Assessment
Tier 1 (MicroDelivery) — complexity ≤ 2, files_affected = 2, criteria_count = 5, no specialist triggers.

## Fixes Required

### Fix 1: modern_robotics.py — Add logging
- Add `import logging` to imports
- Add `_logger = logging.getLogger(__name__)` after all imports
- Replace 2 `print()` calls (matplotlib missing warnings) with `_logger.warning()`

**File:** `src/rotation_converter/modern_robotics.py`

### Fix 2: heavy_integration/test_tools_contracts.py — Add importorskip guards
- Add `pytest.importorskip("trimesh", ...)` at top of each trimesh test method
- Add `pytest.importorskip("ezdxf", ...)` at top of ezdxf test method
- Remove inline `import trimesh` from test_mesh_boolean_intersection (redundant after skip guard)

**File:** `tests/heavy_integration/test_tools_contracts.py`

## Acceptance Criteria Mapping
- AC1: Issue #1688 resolved — 6 test failures fixed
- AC2: TDD — existing tests are the tests (they were written ahead of implementation)
- AC3: ruff check passes on modified files
- AC4: No new print() calls in src/
- AC5: PR created targeting staging
