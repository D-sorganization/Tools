---
id: GH1699
type: execution-plan
tier: 2
created_at: 2026-03-22
---

# GH1699 Execution Plan

## Story
Assessment: Cross-repo integration tests pass but lack Gasification_Model coverage

## Tier
2 — Core Team (Planning + Implementation + QA)

## Files to Change

1. `tests/test_cross_repo_import_compatibility.py`
   - Add `TestGasificationModelProcessCalculatorPatterns` (integration marker)
     covering always-available process_calculators symbols Gasification_Model imports
   - Add `TestContractsDbCUsagePatterns` (contract marker) verifying DbC API surface

2. `tests/heavy_integration/test_tools_contracts.py`
   - Replace bare `import trimesh` with `pytest.importorskip("trimesh", ...)`
   - Replace bare `import ezdxf` with `pytest.importorskip("ezdxf", ...)`

## Acceptance Criteria Mapping

- AC1: Resolve #1699 — covered by new test classes
- AC2: All new code has tests (TDD) — new code IS tests
- AC3: ruff check passes — verified
- AC4: No new print() calls — none introduced
- AC5: PR targeting staging — created in step 8
