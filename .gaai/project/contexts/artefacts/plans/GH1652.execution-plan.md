---
id: GH1652
type: execution-plan
tier: 1
created_at: 2026-03-20
---

# GH1652 Execution Plan — Implement semantic versioning and deprecation path

## Tier
1 — MicroDelivery (complexity ≤ 2, 3 files affected, 5 acceptance criteria)

## Approach

### AC1: Resolve the issue (version stuck at 0.3.0, bump to 1.0.0)
- Edit `pyproject.toml`: `version = "0.3.0"` → `version = "1.0.0"`
- Edit `src/shared/python/programmatic_pid/__init__.py`: `__version__ = "0.3.0"` → `__version__ = "1.0.0"`
- Record in `CHANGELOG.md` [Unreleased] section

### AC2: All new code has tests (TDD)
- Create `src/shared/python/deprecation.py` — the `@deprecated` decorator module
- Create `tests/shared/python/test_deprecation.py` — 14 unit tests
  - Covers: DeprecationWarning emission, message content, return value, functools.wraps, input validation, method usage, version checks

### AC3: `ruff check` passes on modified files
- All files: `ruff check` clean, `ruff format --check` clean

### AC4: No new `print()` calls in `src/`
- New module `deprecation.py` uses `warnings.warn`, no `print()`

### AC5: PR targeting staging
- PR created via `gh pr create --base staging --head story/GH1652`

## Files Affected
| File | Change |
|------|--------|
| `pyproject.toml` | version bump 0.3.0 → 1.0.0 |
| `CHANGELOG.md` | [Unreleased] entries for deprecation.py, version bumps |
| `src/shared/python/deprecation.py` | NEW — @deprecated decorator |
| `src/shared/python/programmatic_pid/__init__.py` | __version__ bump 0.3.0 → 1.0.0 |
| `tests/shared/python/test_deprecation.py` | NEW — 14 unit tests |
