---
id: GH1706
type: execution-plan
story: contexts/artefacts/stories/GH1706.story.md
tier: 1
created_at: 2026-03-22
---

# GH1706 Execution Plan

## Tier

Tier 1 — MicroDelivery (complexity ≤ 2, files_affected = 3, criteria_count = 5, no specialist triggers)

## Approach

Align CI enforcement with the documented 1200-line budget by:
1. Changing `--max-lines 1500` to `--max-lines 1200` in `.github/workflows/ci-standard.yml`
2. Changing `DEFAULT_MAX_LINES = 1500` to `DEFAULT_MAX_LINES = 1200` in `scripts/check_module_size_budget.py`
3. Adding `src/rotation_converter/ui/pyqt6/main_window.py` (1,233 lines) to `config/module_size_budget_baseline.json` as a known exception, since it falls in the 1200-1500 gap zone and is a PyQt6 GUI monolith

Approach rationale: Updating CI to 1200 enforces the documented standard. The `main_window.py` file is already a known exception tracked in prior story GH1690 (story/GH1690 branch, not yet merged). This story independently resolves the discrepancy at the CI level.

## Implementation Steps

1. Edit `.github/workflows/ci-standard.yml` line 135: `1500` → `1200`
2. Edit `scripts/check_module_size_budget.py` line 15: `DEFAULT_MAX_LINES = 1500` → `DEFAULT_MAX_LINES = 1200`
3. Edit `config/module_size_budget_baseline.json`: add `"src/rotation_converter/ui/pyqt6/main_window.py": 1233`
4. Verify: `python3 scripts/check_module_size_budget.py --max-lines 1200 --include src` → PASS
5. Run `ruff check scripts/check_module_size_budget.py` → 0 violations

## Edge Cases

- Any file between 1200-1500 lines not already baselined will now fail CI. Scan showed only `main_window.py` at 1,233 in this range; addressed by step 3.
- Baseline JSON must remain valid JSON after edit.

## Test Checkpoints

- `python3 scripts/check_module_size_budget.py --max-lines 1200 --include src` exits 0
- `ruff check scripts/check_module_size_budget.py` passes
- No `print()` introduced in `src/`

## Risk Register

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| GH1690 PR conflict (also adds main_window.py baseline) | Low | Both PRs add the same entry; merge conflict trivially resolved |
| Other files in 1200-1500 range undiscovered | Low | `wc -l` scan confirmed only one file in gap zone |

## Rollback Boundary

Revert 3 file edits. No schema change, no API change.
