[project]
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
include = ["*"]

[tool.ruff]
target-version = "py310"
select = ["E", "F", "B", "UP", "I"] # Added I for isort
ignore = []

[tool.black]
line-length = 88
target-version = ['py312']
