# Rust toolchain configuration for the Tools repository.
# All contributors and CI will use this version to ensure reproducible builds.
# See: https://rust-lang.github.io/rustup/overrides.html#the-toolchain-file

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
