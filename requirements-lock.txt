# Locked dependency versions for reproducible builds
# Generated: 2026-04-20
# To regenerate: pip install -r requirements.txt && pip freeze > requirements-lock.txt
# See DEPENDENCIES.md for the full dependency pinning policy.
# Regenerate at the start of each sprint cycle or after any requirements.txt change.

# Core Scientific Computing
# Held below 2.4.0 to satisfy requirements.txt's upper bound (numpy 2.4+
# ships PEP 695 syntax in its bundled stubs, which breaks the pinned mypy).
# The lock previously pinned 2.4.6, contradicting the very file it locks (#4014).
numpy==2.3.5
pandas==3.0.3
scipy==1.17.1
matplotlib==3.11.1
sympy==1.14.0

# Testing & Quality Assurance
pytest==9.1.1
pytest-cov==7.1.0
pytest-timeout==2.4.0
pytest-benchmark==5.2.3
ruff==0.15.22
mypy==2.3.0
bandit==1.9.4
types-PyYAML==6.0.12.20260518
types-requests==2.33.0.20260518

# GUI & Desktop Applications
PyQt6==6.11.0
pyqtgraph==0.14.0
pygame==2.6.1
PyOpenGL==3.1.10

# Web Applications
flask==3.1.3

# Security & Utilities
cryptography==49.0.0
defusedxml==0.7.1
PyYAML==6.0.3
pillow==12.3.0
trimesh==4.12.2
