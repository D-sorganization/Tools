# Next: Finite-Grip Spectrum and Stability Qualification

Keep G and C separate. Refactor the existing second-order generator into a shared general velocity-coefficient kernel, retaining a skew-G-only wrapper for the existing clamped API. A damped entry validates real symmetric positive mass, skew G and symmetric PSD C with explicit tolerances; retain nonsymmetric K, growing roots and defective eigenbases. No projection, diagonal repair or artificial damping. Polynomial residual denominators must retain original coefficient norms; residuals and conditioning do not establish forward accuracy near clusters/defects.

Independent two-node axial polynomial: M=[[a,b],[b,d]], K=[[k11,k12],[k12,k22]], C=diag(c,0). Characteristic coefficients descending are [a*d-b*b, c*d, a*k22+d*k11-2*b*k12, c*k22, k11*k22-k12*k12]. Use synthetic EA1000, mu.2, L1, mg.03, mt.1, kg400,c2: a=.03+.2/3,b=.2/6,d=.1+.2/3,k11=1400,k12=-1000,k22=1000. Check four axial roots independently, preserving pole signs/complex conjugacy and all-node displacement/velocity relation.

Continuum Laplace benchmark: kappa=s*sqrt(mu/EA), dg=kg+c*s+mg*s*s. D(s)=EA*kappa*sinh(kappa*L)+dg*cosh(kappa*L)+mt*s*s*(cosh(kappa*L)+dg*sinh(kappa*L)/(EA*kappa)). Solve complex roots with separately checked real/imaginary equations, away from s=0; compare 2/4/8 element low axial poles with this independent distributed rod. State the selected pole and tolerances rather than sorting all bending/torsion modes by magnitude.

Before production edits add RED tests for scalar damped roots, zero-damping equivalence, negative K, the 2D gyroscopic damping-destabilization example, PSD damping with an undamped mode, singular/nonpositive/asymmetric mass, non-skew G, indefinite/asymmetric C, invalid scales, overflow and residual refusal. Add all-node balance/domain and frame retention controls; preserve clamped tests.

Frozen rates remain unqualified as a swing-stability certificate. A separate stability assessment must state autonomous or driven/time-varying coefficients, mass regularity, positive-energy assumptions and the region where the smooth model holds. Positive M/K and PSD C provide a Lyapunov-energy result only under the stated constant/skew assumptions; asymptotic decay requires the largest invariant zero-loss set to be the equilibrium. For nonsymmetric/nonconservative cases or driven trajectories, use the appropriate spectral/nonautonomous analysis with conditioning, transient growth and convergence. Do not turn numerical thresholds into measured golfer uncertainty.

## Next Assessment Contract

Inventory across src finds no reusable linear dynamic stability assessor; the existing physics-validator stability flags concern support polygons. Reuse DampedPencil, SpectrumScales and the shared scaled-generator kernel. Keep FrozenSpectrum.stability_status unchanged. A separate operation must explicitly address the constant, homogeneous, regular finite-dimensional linear ODE; it must not certify the driven swing or nonlinear/contact trajectory.

For a candidate symmetric P, independently recompute Q=-(A^T P+P A) from the original scaled generator. Positive P and Q imply d(x^T P x)/d tau=-x^T Qx. The resulting state-norm envelope is sqrt(lambda_max(P)/lambda_min(P))*exp[-lambda_min(Q)*t/(2*lambda_max(P)*T)]. This is a coordinate-dependent bound, not a physical energy or uncertainty estimate. Record residuals, definiteness margins and conditioning. A failed or unresolved candidate means not established, never an automatic instability verdict. Numerical tests are not interval-verified proof. Do not repair plant coefficients; any symmetrization of a freely chosen candidate P must be explicit and checked against the unchanged plant.

The verified SciPy 1.15.3 API solves A X+X A^H=Q using Bartels-Stewart; use the transposed generator and negative identity for the conventional candidate. [Official API](https://docs.scipy.org/doc/scipy-1.15.3/reference/generated/scipy.linalg.solve_continuous_lyapunov.html). Boyd's [linear quadratic Lyapunov notes](https://web.stanford.edu/class/ee363/notes/lq-lyap-notes.pdf) are an indexed primary planning reference; the full-PDF web reader refused its redirect. Do not claim a full-text review or bypass that refusal.

Write RED tests before implementation. Independent scalar control A=[[0,1],[-1,-1]] has P=[[1.5,.5],[.5,1]] for Q=I. Critical damping A=[[0,1],[-1,-2]] has P=[[1.5,.5],[.5,.5]] despite its defective eigenbasis. Include undamped/neutral systems, the existing damping-destabilized gyroscope, circulatory K, inaccurate solver output, malformed inputs, scale conversion and transient amplification. Exact conservation/PSD-loss arguments need separate stated assumptions; do not silently equate semidefinite loss with asymptotic decay.

## Published Position

Response 12bcf3d83 and spectra 97d46055c are published; turnover bbc27dbe3 is remote-verified and tracked-clean before this planning edit. AffineDrift #4298 merged as d7e51655d. UpstreamDrift #9825 is committed at f0bbc4d50: 128 strict native and 68 claim/release tests pass; publication validation passes at that source. Its first push stopped on missing local h5py; a local environment with repository-locked numerical dependencies repairs collection, and all normal hooks then pass. Source f0bbc4d50 and PR-key turnover e93ef5224 are published in ready PR #9826; protected CI remains running. Follow docs/development/claim_preservation_9825_turnover.md in that worktree. Tools #5072 lease expires 2026-09-09T00:13:36Z; recheck ownership before the next implementation.
