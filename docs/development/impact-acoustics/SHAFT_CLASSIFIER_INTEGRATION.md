# Shaft Branch and Scientific Import Inventory Integration

Tools #5072 integrates main 33144678cb92719c3a1ea87c08ef75fb39fb88ab, including
merged classifier #5103, from published shaft checkpoint
6bf223941c2b369362db5a7b0bb30853a07620be. The inventory producer now recognizes
actual Python scientific imports through syntax inspection without executing
the inspected module. Historical classifier evidence is preserved in
INVENTORY_IMPORT_REVIEW.md and INVENTORY_MAIN_21690_REVIEW.json.

## Exact delta and scientific limits

The canonical generator, not a manual classification edit, produced the merged
inventory. SHAFT_CLASSIFIER_INTEGRATION.json records every transition:

- Before: 3,674 modules, including 875 calculation candidates.
- After: 3,675 modules, including 1,323 calculation candidates.
- All 448 changes are non-calculation to provisional calculation. Every
  transitioned candidate retains blocked publication; none gains approval.
- There are 55 transitions in golf_club. All 55 source hashes are unchanged.
  The newly recognized numerical kernels already existed and were tested;
  this integration does not create new physical or numerical results.
- No module path is removed. The one new path is the classifier's AST helper.
- Two transitioned source hashes elsewhere change through the incoming main
  theme work. Their original and current classification context remains in
  the retained classifier review; no review JSON is rewritten to conceal drift.

All shaft/grip production source, their tests and the public API baselines
remain unchanged by this merge. Root turnover and the progress history were
reconciled explicitly; both prior history sections were preserved. Generated
handoff metadata is reconstructed after actual governance checks.

## Validation

The merged-source Windows run passes 217 tests in 92.83 s with 11 existing
theme deprecation warnings. It includes scientific import cases, strict
inventory schema/freshness/reproduction, the merge driver, incoming palette
architecture, the full theme test directory, all shared API baselines and all
23 frequency-interval controls. The two full inventory scans remain below
their original 60-second individual deadlines (27.14 s and 16.97 s).

Actual mypy 1.13 passes all 11 incoming changed Python files. Repository Ruff
0.14.10 passes, with 3,803 Python files already formatted. All nine manual
gates pass. Normal merge hooks subsequently passed for local commit
5c5fa16b932be819deb0b1988df3d799a473b752. An interrupted push initially left the
remote at 6bf223941. After repository authentication recovered, the retry
completed every normal push hook, including both dependency audits and fleet
guards; the remote SHA is now verified as 5c5fa16b9.

The previously published 920-test Linux scientific/API run still identifies
6bf223941, not a newly executed full Linux run of this merge. The unchanged
scientific source and fresh focused integration evidence are kept distinct.

## Continuation

The #5072 claim was renewed through 2026-09-09T10:57:31Z after GitHub access
recovered. A second authentication interruption at 14:07 UTC prevented renewal;
repository API access recovered at 14:26 UTC and the verified unheld claim was
renewed through 16:27:01 UTC. See the current PROGRESS.md resume note for exact
branch state and later, unintegrated main changes. Continue
continuous-band/full-reduced response qualification,
moving nonlinear boundary work and flexible contact. Calibrated radiation,
exact-pin consumer studies and physical/blinded evidence remain required.
The separate #5114 GUI candidate is published through ea891eed29 with normal
push hooks; its full-suite worker losses remain unresolved and no GUI repair
is included in this scientific branch.
