# Calibration identity scanner finding — Tools #5159

At a76d02d88, run34446788662 / job102773207196 failed its baseline-change check
on the fixed hexadecimal digest in test_calibration_identity.py. The string is
the independently fixed SHA256 of a synthetic waveform, not a credential.
Detect-secrets1.5.0 reproduces one finding before the change and none after a
single next-line allowlist annotation. The digest, legacy-byte assertion and
all other assertions remain unchanged; nine identity tests pass in2.70s.
No scanner baseline, workflow, global exclusion or security setting changes.

Both Linux test aggregates passed at that same head, including all61 new
calibration cases on each version. The current branch also preserves the
published combined provider e83bd2e4 exactly;208 combined waveform/report/API/
shard/context/CodeMap controls pass in19.59s. Source-specific evidence and
scanner/JUnit hashes are in CALIBRATION_RESULTS.json. Normal merge publication
and protected exact-head CI remain required.
