# Adaptive Event CI Type Repair

- Review: https://github.com/D-sorganization/Tools/pull/5152
- Failing source: d212714c1db24e616f22a6b5085885c98a971375
- Run: https://github.com/D-sorganization/Tools/actions/runs/34436796361
- Quality job:102743442327; failure2026-09-10T04:21:51Z.

CI's changed-file mypy invocation reports two no-any-return errors for `np.r_`
results in ContactAbsoluteTolerances.vector and \_Chart.derivative. Its NumPy is
2.3.5 and mypy1.13.0. The previous local module-based and isolated skipped-import
checks passed but did not catch this invocation difference. Matrix jobs were
cancelled after the quality failure; the aggregates are not numerical results.

The repair uses explicitly annotated ndarray locals and returns those same
arrays. No numerical expression, contact law, tolerance or test deadline changes.
The exact changed-file invocation now passes locally on both affected modules;
all15 affected event tests pass in37.40s. Normal hooks and fresh CI remain required. The original386
archive and the later15-control scalar repair retain their source identities.

A documentation push was also refused because the working tree already
contained this newly discovered source repair without a staged SPEC update.
The guard was retained; publish the complete source/SPEC/turnover change together.
No push bypass or empty retry is used.
