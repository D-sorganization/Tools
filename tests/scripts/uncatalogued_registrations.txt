# Registrations under src/ that declare a GUI surface but ship no launcher
# script, so scripts/generate_tools_json.py omits them from tools.json,
# tool_surface_contract.json and the README catalog.
#
# This is a SHRINK-ONLY backlog, not an allowlist. A registration not listed
# here fails test_every_registration_is_catalogued_or_recorded immediately, and
# a listed entry that gains a launcher (or an explicit "catalog_visible": False)
# must be removed from this file. Entries are retired by Tools#4916.
#
# Each line is a path relative to the repository root.
