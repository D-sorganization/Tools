{
  "_comment": "Test modules kept out of the sharded PR lane (Tools #4913). Quarantine individual modules only, never directories; every entry needs an owner cluster and a tracked issue, and python scripts/ci_test_shards.py --check fails when a path no longer exists. Remove entries as they are fixed; #4933 closes when this list is empty.",
  "entries": []
}
