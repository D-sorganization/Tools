[package]
name = "data-processor"
version = "1.0.0"
description = "Data Processor - Signal Processing Desktop Application"
authors = ["D-sorganization"]
license = ""
repository = ""
edition = "2021"

[lib]
name = "data_processor_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
