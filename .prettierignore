# tools.json is generated by scripts/generate_tools_json.py with 4-space indent.
# Prettier must not reformat it or the CI check-manifest job will fail.
tools.json

# This ledger is byte-checked against its canonical Python renderer in CI.
# Formatting it separately changes table/glob output and invalidates freshness.
docs/shared/divergence_ledger.md

# web/src/vendored holds byte-exact copies of canonical monorepo JSON
# (see src/rate_of_closure/web/src/vendored/vendored_map.json). Prettier must
# not reformat them or tests/rate_of_closure/test_web_vendored_sync.py's
# byte-equality gate will fail.
src/rate_of_closure/web/src/vendored/
