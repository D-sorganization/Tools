# Core Scientific Computing
numpy>=2.0.1  # Fundamental array processing and matrix operations
pandas>=2.2.2  # Data analysis, manipulation, and time series
scipy>=1.13.1  # Scientific algorithms (integration, optimization)
matplotlib>=3.9.0  # Data visualization and plotting library
sympy>=1.12  # Symbolic mathematics and algebra

# Testing & Quality Assurance
pytest>=8.2.0  # Testing framework
pytest-cov  # Coverage plugin for pytest (code coverage metrics)
ruff  # Fast Python linter and formatter (replaces flake8/black)
mypy==1.13.0  # Static type checker for Python (pinned to match CI workflow)
types-PyYAML  # Type stubs for PyYAML (static typing)
types-requests  # Type stubs for requests library (static typing)
types-python-dateutil  # Type stubs for python-dateutil (avoids mypy --install-types cache issue)
types-decorator  # Type stubs for decorator (avoids mypy --install-types cache issue)

# GUI & Desktop Applications
PyQt6>=6.7.0  # Qt6 Python bindings for GUI applications
pygame>=2.0.0  # Game development and multimedia library
PyOpenGL>=3.1.5  # OpenGL bindings for Python
PyOpenGL_accelerate>=3.1.5  # Accelerated OpenGL extensions

# Web Applications
flask>=3.0.0  # Web framework for building web applications

# Data Validation & Serialization
pydantic>=2.0.0  # Data validation using Python type annotations

# Security & Encryption
cryptography  # Cryptographic recipes and primitives
defusedxml>=0.7.0  # Safe XML parsing (mitigates XML bomb / XXE attacks)
pygments>=2.18.0,<2.19.0  # Pinned: CVE-2026-4539 affects 2.19.x (transitive via rich/pip-audit)

# Data Formats & Configuration
pyyaml>=6.0.1  # YAML parser and emitter

# Web Automation & Testing
playwright>=1.40.0  # Browser automation and testing framework
