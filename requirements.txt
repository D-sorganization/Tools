# Dependency Pinning Policy: see DEPENDENCIES.md
# Strategy: >= minimum bounds here; exact pins in requirements-lock.txt
# Regenerate lock: pip install -r requirements.txt && pip freeze > requirements-lock.txt

# Core Scientific Computing
numpy>=2.0.1,<2.4.0  # Fundamental array processing and matrix operations; numpy 2.4+ ships PEP 695 syntax in its bundled __init__.pyi stub, which crashes mypy 1.13.0's incremental cache serializer (AssertionError: unresolved placeholder type) under this repo's python_version=3.11 mypy config
pandas>=2.2.2  # Data analysis, manipulation, and time series
pytz>=2026.2  # Timezone library required by pandas>=2.3.1
scipy>=1.13.1,<1.18  # Scientific algorithms (integration, optimization)
matplotlib>=3.9.0  # Data visualization and plotting library
sympy>=1.12  # Symbolic mathematics and algebra

# Testing & Quality Assurance
pytest>=9.0.3  # Testing framework
pytest-qt>=4.4.0  # Pytest plugin for PyQt applications
pytest-cov>=4.0.0  # Coverage plugin for pytest (code coverage metrics)
jsonschema>=4.26.0  # Draft 2020-12 validator for the swing_sim ground profile schema tests
pypdf>=6.16.2,<7.0.0  # Deterministic PDF normalization and semantic design-manual QA
ruff>=0.1.0  # Fast Python linter and formatter (replaces flake8/black)
mypy==1.13.0  # Static type checker. ONE pin (Tools #4913): ci-standard.yml, benchmark-suite.yml, nightly-full-repo-quality.yml and Jules-PR-AutoFix.yml install this exact version; bump them together
types-PyYAML>=6.0.12.20260815  # Type stubs for PyYAML (static typing)
types-requests>=2.33.0.20260518  # Type stubs for requests library (static typing)
types-python-dateutil>=2.9.0.20260807  # Type stubs for python-dateutil (avoids mypy --install-types cache issue)
types-decorator>=5.2.0.20260712  # Type stubs for decorator (avoids mypy --install-types cache issue)

# GUI & Desktop Applications
PyQt6>=6.7.0  # Qt6 Python bindings for GUI applications
pyqtgraph>=0.13.7  # Plot widgets used by P1AM desktop Control/Trends tabs
pygame>=2.0.0  # Game development and multimedia library
PyOpenGL>=3.1.10  # OpenGL bindings for Python
PyOpenGL_accelerate>=3.1.10  # Accelerated OpenGL extensions

# Web Applications
flask>=3.0.0  # Web framework for building web applications
flask-limiter>=3.5.0  # Rate limiting for Flask apps (used by calculator API tests)
fastapi>=0.141.1  # High-performance web framework for APIs
starlette>=0.45.0  # ASGI framework underlying FastAPI

# Data Validation & Serialization
pydantic>=2.0.0  # Data validation using Python type annotations
pydantic-settings>=2.0.0  # Settings management for Pydantic v2
filelock>=3.12.0  # File locking mechanism for concurrent state security and authority jobs

# Security & Encryption
cryptography>=50.0.1  # Cryptographic recipes and primitives
defusedxml>=0.7.0  # Safe XML parsing (mitigates XML bomb / XXE attacks)
pygments>=2.21.0,<2.22.0  # Pinned: CVE-2026-4539 affects 2.19.x (transitive via rich/pip-audit)

# Data Formats & Configuration
pyyaml>=6.0.1  # YAML parser and emitter

# Web Automation & Testing
playwright>=1.61.0  # Browser automation and testing framework
