"""Setup script for Tools shared library.

This file exists for compatibility with older build systems.
Modern builds should use: pip install -e .
which reads configuration from pyproject.toml.
"""

from setuptools import setup

setup()
